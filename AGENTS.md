# Map Vendor 开发代理规范

本文件约束 `D:\mapvendor` 及其全部子目录中的开发代理。开始任何需求分析、设计、编码、数据库变更或评审前，必须先阅读本文件和项目根目录中的 `地图商家预约系统_后端与数据库PRD_v1.7.docx`。

## 0. Git 仓库

- 项目仓库：`https://github.com/qq691082695-arch/map.git`。
- 本地仓库根目录：`D:\mapvendor`；默认分支：`main`；远程名称：`origin`。
- 后端位于 `Map-Vendor/`，现有前端位于 `TripAgency/TripAgency/`，产品与代理文档位于仓库根目录。
- 提交前必须检查 `git status`，不得提交构建产物、依赖目录、IDE 配置、临时渲染文件、压缩备份或真实密钥。
- 未经用户明确要求，不得强制推送、重写远程历史或删除远程分支。

## 1. 产品与角色边界

- 产品只包含两类使用者：微信小程序用户、平台管理员。
- “商家”是由平台管理员维护的服务商业务数据，不是系统角色，不拥有账号、后台、登录或权限体系。
- 产品包含微信小程序、平台管理员后台、Java 后端和 MySQL 数据库；不建设商家独立后台。
- 本项目代理当前只负责 Java 后端、MySQL、文件存储、OpenAPI、测试和部署；微信小程序及管理员后台前端由其他开发人员负责。
- 未经用户明确提出前端修改请求，不得修改 `TripAgency-web` 或 `TripAgency-uniapp`；后端窗口只提供接口契约、联调说明和可验证接口。
- MVP 不包含支付、退款、库存锁定、评价、司机端、微信服务通知、地图框选或地址自动解析。
- 订单只有 `PENDING`、`CONFIRMED`、`CANCELLED` 三种状态，不得增加“已拒绝”等状态。
- 待确认订单可由对应用户取消；管理员确认后用户不可取消。管理员不接受预约时转为已取消并填写原因。
- 统计以 `service_date` 为业务日期，分别统计三种状态和合计，不得默认改用创建时间。

## 2. 固定技术基线

- Java：JDK 1.8；禁止使用 Java 9+ API、语法或运行时要求。
- Spring Boot：2.7.x；校验包使用 `javax.validation`。
- 数据库：MySQL Community Server 9.0.1，本机服务 `MySQL90`，端口 3306，`utf8mb4`，严格 SQL mode。
- 数据库结构全部由 Flyway 迁移维护，不依赖人工修改线上表。
- 后端采用单体分层/模块化架构；未经明确批准不得拆微服务。
- MVP 不强制 Redis；没有性能证据时不增加 Redis、MQ、Elasticsearch。

## 3. 小程序用户身份边界

- uni-app 在微信小程序启动或订单功能需要身份时静默调用 `uni.login`/`wx.login` 获取一次性 code，不展示登录页面。
- Java 后端只允许通过 `POST /api/v1/app/wechat/session` 调用微信 `code2Session`，仅向前端返回 `openid`；不得返回或持久化 `session_key`，不得建立 Session、JWT、刷新令牌或小程序账号密码体系。
- 微信 AppSecret 只能通过服务端环境变量配置，禁止写入 uni-app、仓库通用配置或日志；生产环境缺少 AppID/AppSecret 时必须拒绝启动。
- uni-app 缓存后端返回的 `openid`，App 订单接口继续接收并以其作为订单归属辨别字段。
- `wx_user` 仅作 openid 内部映射/记录，不代表后端登录系统。
- 静默交换不等于订单强认证：现有订单接口仍信任客户端传入的 openid，存在重放或篡改风险；不得描述为完整认证，必须使用 HTTPS、交换接口限流和日志脱敏并记录风险。
- 手机号不能替代 openid 作为订单归属字段。

## 4. 平台管理员业务边界

- 管理员后台登录完全由前端完成；Java 后端不提供管理员账号、登录、改密、密码校验、Session、JWT 或管理端令牌。
- `/api/v1/admin/**` 当前不做后端登录认证；必须在风险说明中记录该边界，并使用 Nginx 网络访问限制、HTTPS、限流和审计日志降低暴露风险。
- 管理员可管理高校、全部服务商、车辆/菜品/房型、文件资源、全部订单、全局统计和导出。
- 管理员可确认或取消待确认订单；取消必须填写原因并记录 `ADMIN` 来源。
- 管理员后台默认分页，所有关键写操作需要 requestId 和审计日志。
- 不存在商家账号、商家认证上下文、商家数据权限或 `/api/v1/merchant/**`。

## 5. 服务商业务数据

- `business` 表示服务商，不表示登录主体。
- 服务商分为 `TRAVEL`、`HOTEL`、`FOOD`，全部由管理员创建、编辑、启停和逻辑删除。
- 出行服务商拥有车辆；住宿服务商拥有房型；餐饮服务商拥有菜品。
- 附属资源必须属于正确类型的服务商。
- 禁用或删除的服务商不出现在小程序地图和详情中，不能创建新预约；历史订单仍可查看。
- 历史订单保存服务商和车辆/房型等展示快照，主数据变化不得导致历史失真。

## 5.1 现有前端基线与协作边界

- 管理员后台位于 `TripAgency/TripAgency/TripAgency-web`，技术栈为 Vue 3、Vite、Element Plus、Pinia 和 Vue Router。
- 已存在登录页、仪表盘、服务商管理、高校管理和订单管理页面；当前主要通过 `src/api/mock.js` 使用内存假数据，`src/api/index.js` 尚未接入真实后端。
- 管理员前端登录使用前端固定演示账号和本地 token，仅属于前端实现，不得据此新增后端登录接口。
- 已有服务商页面包含价格、单位、酒店面积/早餐等展示字段，订单页面包含金额；这些字段与当前后端口径存在差异，后端不得未经确认擅自新增数据库字段。
- 后端每完成一个业务窗口，应同步更新 OpenAPI、字段映射和联调说明，交由前端开发人员接入；不得把“前端页面完成”作为本后端窗口的实现内容。
- 前端联调发现契约冲突时，先更新字段契约并由用户确认；涉及表结构时必须同步 PRD、Flyway、OpenAPI 和测试。

## 6. 预约字段与状态机

三类订单公共必填字段：

- `openid`
- `contact_name`
- `contact_phone`
- `people_num`，正整数
- `service_date`
- `business_id`
- `business_name_snapshot`
- `service_type`

分类字段：

- 出行：车辆 ID/规格快照、车辆数量、`DAY_CHARTER` 或 `ROUND_TRIP`。
- 住宿：房型 ID/规格快照、房间数量。
- 餐饮：`BREAKFAST`、`LUNCH`、`DINNER`，不设计具体时段。

状态规则：

- 创建订单固定为 `PENDING`。
- `PENDING -> CONFIRMED`：仅平台管理员确认。
- `PENDING -> CANCELLED`：所属 openid 用户或平台管理员取消；管理员取消原因必填。
- `CONFIRMED`、`CANCELLED` 都是终态。
- 状态更新必须使用条件更新或乐观锁，并与 `order_status_log` 同事务。

核心筛选、统计、关联和校验字段必须结构化；`extra_json` 不得承载服务日期、人数、状态或主要选择项。

## 7. 数据库目标结构

- 目标业务表包括：`wx_user`、`university`、`business`、三类服务商子表、`file_resource`、文件关联表、`reserve_order`、`order_status_log`、`api_idempotency`。
- 目标结构不包含 `sys_admin` 或 `merchant_account`；现有 `V001__baseline.sql` 中两表属于已取消的后端登录体系遗留，必须通过新的 Flyway 迁移处理，不得直接改已执行的 V001。
- `cancel_source` 目标枚举只有 `USER`、`ADMIN`；`MERCHANT` 属于遗留值，必须在迁移与代码清理窗口处理。
- `order_status_log.operator_type` 目标值可包含 `USER`、`ADMIN`、`SYSTEM`，不再包含 `MERCHANT`。
- `reserve_order.order_no` 唯一；`openid` 非空；保留 `(openid, created_at)`、`(service_date, status, business_type)`、`(business_id, service_date)` 等索引。
- 高校、服务商及被订单引用的附属资源使用逻辑删除；订单不提供普通删除接口。
- 图片使用 `file_resource` 和明确关联表，不得逗号拼接，不在数据库保存二进制正文。
- 任何结构调整必须新增 Flyway 迁移并说明兼容/回滚策略。

## 8. API 命名空间

- `/api/v1/admin/**`：管理员后台业务接口；当前不实现后端登录或认证。
- `/api/v1/app/**`：小程序接口；只允许 `/api/v1/app/wechat/session` 承担一次性 code 到 openid 的静默交换，不建立服务端登录态。
- 禁止创建 `/api/v1/merchant/**` 或商家登录接口。
- 统一响应包含 `code`、`message`、`data`、`requestId`。
- 列表默认分页；`pageSize` 最大 100；排序必须稳定。
- 创建订单支持 `Idempotency-Key`。
- 日期使用 `YYYY-MM-DD`；业务时区为 `Asia/Shanghai`。

## 9. 文件存储

- 首期使用现有服务器约 40GB 本地空间。
- 文件写入配置指定的独立数据目录，禁止写入源码或 Jar 目录。
- 数据库保存相对存储键、访问 URL、MIME、大小和校验值。
- 仅允许 JPEG、PNG、WebP；校验真实 MIME、大小、扩展名；随机文件名；阻止路径穿越。
- 保留 `StorageService` 抽象，并配置容量告警、备份和清理策略。

## 10. 推荐代码结构

```text
src/main/java/<base>/
  common/
  module/admin/
  module/university/
  module/business/
  module/order/
  module/statistics/
  integration/storage/
  integration/export/
src/main/resources/db/migration/
src/test/java/
deploy/
```

- 不再新增 `module/merchant` 业务代码；现有商家角色遗留应在清理窗口删除或迁移。
- Controller 只处理协议、参数校验和请求上下文，不实现登录认证上下文。
- Service/Application 负责权限、事务、状态机和用例编排。
- Repository/Mapper 负责数据库访问，Controller 不得直接调用 Mapper。

## 11. 修改工作流

1. 阅读 PRD、本文件和现有代码，检查工作区未提交修改。
2. 定位受影响规则、接口、表和测试，不立即大范围重构。
3. 涉及数据库时先设计 Flyway 迁移和兼容/回滚策略。
4. 实现最小范围变更，保持 JDK 1.8。
5. 更新 OpenAPI、字段字典和相关测试。
6. 至少运行受影响模块测试；状态机、幂等和并发必须有集成测试。
7. 交付报告修改文件、迁移、测试、风险和人工配置。

## 12. 必测场景

- 后端不存在任何账号密码校验、Session、JWT、管理端令牌、商家账号、商家登录或 `/merchant/**`；微信能力仅限一次性 code2Session 交换接口，且不返回 session_key。
- `/admin/**` 当前不做后端登录认证；部署时验证 Nginx 网络边界、HTTPS、限流和审计配置。
- 待确认订单并发确认/取消只能有一个成功。
- 已确认订单由用户取消必须失败。
- 车辆和房型必须属于订单服务商，数量大于 0。
- 管理员取消必须填写原因并记录 `ADMIN`。
- 禁用服务商不能被预约，历史订单仍可查看。
- 统计按 `service_date` 和三种状态汇总。
- 重复 `Idempotency-Key` 不产生第二张订单。
- 非法、超限、伪装 MIME 和路径穿越文件被拒绝。

## 13. 禁止事项

- 禁止升级到 JDK 17 或 Spring Boot 3.x。
- 禁止新增任何后端登录流程、密码校验、Session、JWT 或令牌体系。
- 禁止新增商家角色、商家账号、商家后台、商家登录、商家令牌或 `/merchant/**`。
- 禁止增加未确认的订单状态或允许用户取消已确认订单。
- 禁止将关键预约字段放入 JSON 替代结构化列。
- 禁止在生产配置或通用配置中硬编码密钥，也禁止日志记录完整手机号/openid；仅本机开发使用的 `application-local.yml` 可保存固定开发数据库密码 `ninnikuuu.`，该例外不得复制到其他 profile 或生产部署；后端不得新增账号密码和登录令牌。
- 禁止直接把 8080 暴露公网；生产流量通过 Nginx 和 HTTPS。
- 禁止无迁移脚本直接改变数据库结构。
- 禁止为了未来可能需要而引入微服务、MQ 或复杂中间件。

如需求与本文件冲突，停止实现并请求确认；确认后同步更新 PRD、数据库设计、OpenAPI、测试和本文件。

## 14. 当前有效的分窗口开发队列

一个窗口只完成一个编号任务。每个窗口开始时重新检查代码、迁移和测试；不得仅凭编号假定前序完成。用户只说“继续下一步”时，选择首个未达到验收标准的窗口。

### 阶段 01：范围与契约纠正

#### 窗口 01A：两角色范围冻结

- 将角色冻结为小程序用户、平台管理员。
- 明确服务商只是业务数据，不是角色。
- 更新字段字典、OpenAPI 和安全说明，删除商家角色契约。
- 核对 App 地图、详情、预约和订单 DTO。
- 盘点现有管理员后台的 dashboard、服务商、高校、订单页面与 mock API，形成前端字段到后端 DTO 的映射；不修改前端代码。

验收：PRD、AGENTS、字段字典和 OpenAPI 不再描述商家登录或商家后台。

#### 窗口 01B：数据库与代码遗留清理设计

- 盘点 `merchant_account`、`MERCHANT` 枚举、`/merchant/**`、`module/merchant`、商家认证与测试遗留。
- 设计后续 Flyway 迁移，处理 `merchant_account`、取消来源和状态日志约束。
- 不修改已执行的 `V001`；明确空库和已迁移数据库兼容策略。

验收：形成可执行的迁移和代码清理清单，无数据丢失或约束顺序歧义。

### 阶段 02：登录遗留清理与公共基础骨架

#### 窗口 02A：全量登录认证遗留清理设计

- 盘点管理员与商家登录接口、密码字段、JWT/Session、令牌过滤器、Spring Security 登录规则和相关测试。
- 明确管理员后台登录仍由前端完成；小程序由前端静默取 code，后端仅交换 openid，不建立登录态。
- 设计删除 `sys_admin`、`merchant_account`、`token_version` 和登录认证代码的 Flyway/代码兼容方案。
- 记录 `/admin/**` 无后端认证的风险与 Nginx 网络访问限制、HTTPS、限流、审计要求。

验收：形成完整遗留清单；PRD、AGENTS 与 OpenAPI 不再安排任何后端登录开发。

#### 窗口 02B：角色遗留数据库迁移与代码清理

- 新增 Flyway 迁移清理 `sys_admin`、`merchant_account`、登录令牌字段和 `MERCHANT` 数据库约束。
- 删除管理员/商家登录接口、密码与令牌代码、Java `CancelSource.MERCHANT`、Principal/Scope、商家模块占位和相关安全规则。
- 更新基线契约测试、OpenAPI 和部署校验脚本。

验收：目标 MySQL 9.0.1 迁移成功；全库和源码无可调用的商家角色入口；测试通过。

### 阶段 03：管理员后台内容管理

#### 窗口 03A：高校管理后端

- 实现高校分页、详情、新增、编辑、启停、逻辑删除和多边形校验。
- 管理高校图片关联，稳定排序。
- 补齐 OpenAPI 和数据库集成测试。

验收：高校 CRUD、可见性和多边形边界测试通过。

#### 窗口 03B：服务商管理后端

- 实现服务商分页、详情、新增、编辑、启停和逻辑删除。
- 校验类型、经纬度、名称、地址和历史订单引用。
- 明确服务商没有账号、登录或权限字段。

验收：三种服务商 CRUD 和逻辑删除测试通过。

#### 窗口 03C：车辆、房型、菜品管理后端

- 实现三类附属资源分页、详情、CRUD、启停和逻辑删除。
- 校验资源所属服务商和服务商类型。
- 被订单引用的资源不通过普通接口物理删除。

验收：类型错属、跨服务商资源和逻辑删除测试通过。

#### 窗口 03D：文件上传与资源关联

- 实现管理员图片上传和高校、服务商、车辆/房型/菜品关联。
- 使用 `StorageService`，校验真实 MIME、大小、扩展名和路径穿越。

验收：全部文件安全用例通过，数据库只保存元数据。

### 阶段 04：管理员订单管理

#### 窗口 04A：订单列表、筛选与详情后端

- 实现管理员全量订单分页、服务日期/状态/类型/服务商筛选和详情。
- 使用稳定排序和订单快照展示历史。

验收：筛选、分页、快照和敏感字段规则测试通过。

#### 窗口 04B：管理员确认、取消与状态机

- 实现管理员确认和取消待确认订单。
- 取消原因必填，`cancel_source=ADMIN`。
- 条件更新或乐观锁；状态与日志同事务。
- 增加并发、重复处理和终态测试。

验收：并发只有一个成功；无 `MERCHANT` 操作来源；日志完整。

### 阶段 05：管理员统计与导出

#### 窗口 05A：管理员统计后端

- 按 `service_date`、服务商、类型和三种状态统计并返回合计。
- 使用 `Asia/Shanghai` 自然日边界和订单快照。

验收：统计与订单明细一致，日期边界测试通过。

#### 窗口 05B：Excel 导出后端

- 复用订单筛选器，实现流式 Excel。
- 限制最大行数和时间范围，落实手机号权限和导出审计。

验收：导出与列表一致，超限和大数据量测试通过。

### 阶段 06：小程序公开查询

#### 窗口 06A：地图聚合查询后端

- 实现 `GET /api/v1/app/map-overview?type=`。
- 返回启用高校区域和启用服务商轻量点位；类型筛选；避免 N+1。

验收：只返回启用未删除数据，查询和集成测试通过。

#### 窗口 06B：服务商详情查询后端

- 实现 `GET /api/v1/app/businesses/{id}`，返回 `common + detail`。
- 分别返回启用车辆、房型或菜品，不暴露管理字段和物理路径。

验收：三种详情和不可见性测试通过。

### 阶段 07：小程序预约创建

#### 窗口 07A：预约创建后端

- 实现 `POST /api/v1/app/orders`、`Idempotency-Key`、请求哈希和并发防重。
- 校验服务商、日期、类型字段和车辆/房型归属。
- 保存快照，创建 `PENDING` 订单及初始日志。

验收：重复提交不产生第二张订单，资源错属和并发测试通过。

### 阶段 08：用户订单查询与取消

#### 窗口 08A：App 订单查询与取消后端

- 实现按 openid 分页、详情和用户取消。
- 只有 `PENDING` 可取消；状态和日志同事务。

验收：归属、终态、重复取消和并发测试通过。

### 阶段 09：全链路验收与部署

#### 窗口 09A：自动化回归与契约检查

- 运行单元、MySQL 集成、管理员业务接口、状态机、幂等、并发、文件安全和迁移测试。
- 确认源码、数据库目标结构、OpenAPI 和路由中不存在商家角色系统。

验收：第 12 节必测场景全部通过。

#### 窗口 09B：MySQL 与 Flyway 验证

- 在 MySQL 9.0.1 验证空库和 V001 后升级路径。
- 检查索引、外键、CHECK、字符集、严格模式和账号权限。

验收：迁移可重复验证，无人工改表。

#### 窗口 09C：部署上线收口

- 完成 Nginx/HTTPS、环境变量、备份恢复、容量告警、健康检查、日志和回滚说明。
- 向前端负责人交付 OpenAPI、字段映射、环境地址和联调清单，并配合验证小程序及管理员后台主要链路；本窗口不修改前端代码；公网不暴露 8080。

验收：部署与回滚可复现，生产前验证通过。
